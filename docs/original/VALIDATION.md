# 展示包驗證（2026-09-13）

## 最新首頁調整：空白起始

使用者要求首頁不顯示專案影片後，已改成每次開頁／重新整理都保持空白，不再自動恢復舊素材。歷史檔案與工作沒有刪除，仍可主動從紀錄查看。`app/check_empty.cjs` 實測通過：舊 localStorage 不會帶入影片、沒有自動請求範例／過去素材、中央上傳按鈕可操作、上傳後只顯示本次素材並啟用編輯、重新整理回到空白、無 JavaScript 錯誤。下方「重新整理保留結果」是先前版本的測試紀錄，已由此新行為取代。

以下為打包當日紀錄；2026-09-15 新 UI 的驗證另見本文末段，不能把舊版三模式測試當作新版測試。

- 本機網頁、三種模式切換、人物點選、歷史工作、影片 Range 拖曳播放：通過。無 JavaScript 執行錯誤。
- V58 快速重現：已實際完成，工作 `f200e0bd04aa4b5abc61e7e6e8a91f6e`，結果為 `jobs/<ID>/result.mp4`。
- SAM2 → V57 背景修補 → V58 Balanced：已實際完成，工作 `578919bf87d2485581f1430a3e9ac400`。49 幀追蹤完整，人物核心編碼前最大差異為 0，修補覆蓋平均 52.879%。
- 完整新生成的前處理：已驗證原片取樣、5 張候選關鍵幀／3 張初始 memory、文字計畫、SAM2、時間聯集遮罩與首尾條件準備完成。
- StoryMem/Wan2.2、SAM2、CUDA 與必要 Python 相依載入：通過。
- 模型及素材副本：SHA-256 結果請見 `COPY_VERIFICATION.json`。此報告同時記錄來源位置，僅供稽核；正常啟動不讀來源資料夾。
- Docker runtime 備份：既有 image ID `sha256:4e19c8204c49f78137c8945f2480ce707043cf2a3eb54cebb473581553226419`，備份為 `runtime/vcme-storymem.tar`。

## 尚未完成的驗證

此次打包沒有重新執行耗時的完整 40-step Wan2.2 GPU 生成。因此完整新生成入口已接好，前處理和相依載入已通過，但其端到端生成品質尚待實跑驗收；不能用本次兩個後處理成功工作宣稱新生成已驗證。
也未在第二台電腦上驗證 Docker archive 匯入與 GPU 相容性。

SAM2 本次出現可選 `_C` extension 缺少的警告，仍完成全部 49 幀分割；可選遮罩孔洞後處理未執行，記錄保留在工作 log。
V58 舊 EPE 指標僅保留為時間取樣工具的歷史診斷；不存在有效的 2.05 自動通過門檻。本版另外輸出 source-relative C1/C2，仍需人工觀看。

## 2026-09-15：可上傳影片的編輯器

- 六項 host 單元測試通過：動態幀數、非首幀點選參數、非法區間／座標、生成必須具備遮罩、路徑穿越拒絕、工作恢復時的成功／失敗判定。
- Playwright 真實瀏覽器測試通過，無 JavaScript 執行錯誤。實際匯入鏡像測試片，選取 1.00–3.50 秒（2.5 秒），在 2.25 秒點人物。
- 真實 SAM2 工作 `42362e0717de479e9dcbe02adf4aa6ac` 完成：41 幀，prompt frame=20，双向追蹤。可見綠色人物遮罩；非靜態示意圖。來源為 `media/<本次ID>/source.mp4`，不是 assets 範例。
- 更改結束時間後 UI 禁止沿用舊遮罩生成；直接呼叫 API 使用不一致區間也回傳 400。
- 影片 Range 拖曳播放驗證通過。報告：`runtime/EDITOR_TEST_REPORT.json`；截圖：`runtime/EDITOR_PREVIEW.png`。
- 額外的純 UI 互動測試通過：實際拖曳時間軸右把手，結束時間從 5.00 改為 5.55 秒；說明視窗可開關，1366 px 與 820 px 版面已截圖檢查。此測試僅隱藏忙碌狀態測試 UI，不提交生成工作。
- 比對打包時 SHA-256 紀錄：StoryMem 引擎、src、scripts、assets 範圍共 59 個檔案全部相符，確認此次 UI 改版沒有修改這些核心與歷史素材。模型權重本次未重新全量計算 hash，亦未寫入。
- 完整 40-step 新生成測試工作 `efad161a745544f5be498ac6c99b09c2` 已完成，`status.json` 為 done、exit_code=0。依序完成 Native StoryMem/Wan2.2、41 幀生成區人物追蹤、V57、V58、H.264 輸出及 C1/C2 評估；不是用歷史 V58 冒充新生成。
- 輸出解碼驗證通過：來源與輸出均為 8 秒、240 幀、30 fps、622×854。修改時間為 1.00–3.50 秒。抽樣在區間外 0.8 與 3.7 秒的平均像素差約 2.14／2.24（含重編碼）；這不是逐像素無損拼接。圖與資料在該工作 `comparison.jpg`、`output_verification.json`。
- 真實瀏覽器完成驗證通過：生成期間重複提交被拒絕 409、完成後預覽與匯出指向本次 job、重新整理後仍能找到結果，無 JavaScript 錯誤。報告：`runtime/EDITOR_DELIVERY_TEST.json`。
- 本次接點 source-relative flow EPE：開始 0.1829、結束 0.8165；這些是單次診斷數值，沒有足夠依據可當通用合格門檻。比較圖確認人物動作被修改，但手部與自然度仍需正常播放人工驗收。
- 本次測試影片無音軌；輸出沿用原音軌的命令已加入，但此測試不構成帶音訊素材驗證。
- 沒有證明任意影片泛化、多人遮擋穩定性、10 秒生成品質或 V58 等級無痕接合。上傳測試片是範例原片的鏡像，不是不同場景資料集。
